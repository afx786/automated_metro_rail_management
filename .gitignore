# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
*.egg-info/
.pytest_cache/

# Node / frontend
node_modules/
dist/

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
__MACOSX/

# Archives / binaries / data artifacts
*.zip
*.db
*.sqlite3
*.joblib
*.pkl
*.pt
*.h5
# ...except the trained ML artifacts in models/, which are intentionally committed
# so a fresh clone starts with real trained models (no manual retrain step needed).
!kmrl_planner_with_real_maximo/models/*.joblib
!kmrl_planner_with_real_maximo/models/*.pkl

# Runtime / local config
temp_uploads/
.env
.env.local
